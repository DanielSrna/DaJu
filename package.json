{
  "name": "mainplataform",
  "version": "0.1.0",
  "private": true,
  "description": "MainPlataform API (Express + TypeScript + MongoDB) — repositorio backend-only",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "globals": "^17.11.0",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.68.0"
  },
  "lint-staged": {
    "backend/src/**/*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "engines": {
    "node": ">=20"
  }
}
